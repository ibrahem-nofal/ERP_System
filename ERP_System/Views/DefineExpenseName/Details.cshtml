@model ERP_System.Models.ExpenseName

@{
    ViewData["Title"] = "تفاصيل مسمى المصروف";
}

<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"] - @(Model?.Name ?? "")</title>
    <style>
        :root {
            --bg: #0f1724;
            --panel: rgba(255,255,255,0.04);
            --panel-2: rgba(255,255,255,0.02);
            --text: #e6eef6;
            --muted: #94a3b8;
            --accent: #06b6d4;
            --accent-2: #7c3aed;
            --gradient: linear-gradient(90deg,var(--accent),var(--accent-2));
            --radius: 12px;
            --card-shadow: 0 4px 20px rgba(0,0,0,0.3);
        }

        html, body {
            height: 100%;
        }

        /* body {
            background: var(--bg);
            color: var(--text);
            font-family: "Segoe UI", Tahoma, sans-serif;
            margin: 0;
            padding: 30px;
            direction: rtl;
        } */

        .container {
            max-width: 1000px;
            margin: 0 auto;
        }

        h1 {
            text-align: center;
            margin-bottom: 24px;
            font-size: 30px;
            background: var(--gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            font-weight: 700;
        }

        .details-card {
            background: var(--panel);
            padding: 26px;
            border-radius: 12px;
            box-shadow: var(--card-shadow);
            border: 1px solid rgba(255,255,255,0.04);
        }

        .item-header {
            display: flex;
            align-items: center;
            gap: 20px;
            margin-bottom: 18px;
            padding-bottom: 12px;
            border-bottom: 1px solid rgba(255,255,255,0.06);
        }

        .item-icon {
            width: 120px;
            height: 120px;
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 44px;
            background: linear-gradient(135deg,#334155,#0f1724);
            border: 3px solid var(--accent);
            color: #ffffff;
            flex-shrink: 0;
        }

        .item-basic h2 {
            margin: 0 0 6px 0;
            color: var(--accent);
            font-size: 22px;
        }

        .item-basic p {
            margin: 2px 0;
            color: var(--muted);
        }

        .details-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 18px;
            margin-top: 18px;
        }

        .detail-item {
            background: var(--panel-2);
            padding: 14px;
            border-radius: 8px;
            border-right: 3px solid var(--accent-2);
            min-height: 64px;
        }

        .detail-label {
            font-weight: 700;
            color: var(--accent);
            margin-bottom: 6px;
        }

        .detail-value {
            color: var(--text);
            font-weight: 600;
        }

        .description-block {
            background: var(--panel-2);
            padding: 16px;
            border-radius: 8px;
            margin-top: 18px;
            border: 1px solid rgba(255,255,255,0.03);
            color: var(--muted);
            line-height: 1.6;
        }

        .actions {
            display: flex;
            gap: 12px;
            justify-content: center;
            margin-top: 22px;
            flex-wrap: wrap;
        }

        .btn {
            display: inline-block;
            padding: 12px 22px;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 700;
            cursor: pointer;
            border: none;
        }

        .btn-primary {
            background: var(--gradient);
            color: #021024;
            box-shadow: 0 6px 18px rgba(12,46,86,0.25);
        }

        .btn-secondary {
            background: rgba(255,255,255,0.06);
            color: var(--text);
            border: 1px solid rgba(255,255,255,0.06);
        }

        .badge {
            display: inline-block;
            padding: 6px 12px;
            border-radius: 6px;
            font-size: 14px;
            font-weight: 700;
        }

        .badge-active {
            background: #10b981;
            color: #ffffff;
        }

        .badge-inactive {
            background: #ef4444;
            color: #ffffff;
        }

        @@media (max-width: 900px) {
            .item-header

        {
            flex-direction: column;
            align-items: flex-start;
            gap: 12px;
        }

        .item-icon {
            width: 110px;
            height: 110px;
        }

        }
    </style>
</head>
<body>
    <div class="container">
        <h1>تفاصيل مسمى المصروف</h1>

        <div class="details-card">
            <div class="item-header">
                <div class="item-icon">💼</div>

                <div class="item-basic">
                    <h2>@(Model?.Name ?? "-")</h2>
                    <p><strong>المعرف:</strong> #@(Model?.Id.ToString() ?? "-")</p>
                </div>
            </div>

            <div class="details-grid">
                <div class="detail-item">
                    <div class="detail-label">الاسم</div>
                    <div class="detail-value">@(Model?.Name ?? "-")</div>
                </div>

                <div class="detail-item">
                    <div class="detail-label">الوصف</div>
                    <div class="detail-value">
                        @{
                            var desc = Model?.Description;
                        }
                        @( !string.IsNullOrWhiteSpace(desc) ? desc : "-" )
                    </div>
                </div>

                @* لو موديل ExpenseName عنده خصائص إضافية تقدر تعرضها هنا.
                   حالياً نعرض فقط الحقول الأساسية (Name, Description, Id) كما طلبت. *@
            </div>

           

            <div class="actions">
                <a class="btn btn-primary" href="@Url.Action("Edit", new { id = Model?.Id })">تعديل البيانات</a>
                <a class="btn btn-secondary" href="@Url.Action("List")">العودة للقائمة</a>
            </div>
        </div>
    </div>
</body>
</html>
