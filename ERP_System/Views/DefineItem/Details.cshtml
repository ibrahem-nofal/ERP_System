@model ERP_System.Models.Item
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>تفاصيل المنتج</title>
    <style>
        body {
            background: #0f1724;
            color: #e6eef6;
            font-family: "Segoe UI", Tahoma, sans-serif;
            margin: 0;
            padding: 30px;
            direction: rtl;
        }

        .container {
            max-width: 1000px;
            margin: 0 auto;
            background: rgba(255,255,255,0.04);
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.3);
        }

        h1 {
            text-align: center;
            margin-bottom: 30px;
            background: linear-gradient(90deg, #06b6d4, #7c3aed);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .item-header {
            display: flex;
            align-items: center;
            gap: 20px;
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 2px solid rgba(255,255,255,0.1);
        }

        .item-image {
            width: 150px;
            height: 150px;
            border-radius: 12px;
            object-fit: cover;
            border: 3px solid #06b6d4;
        }

        .item-basic {
            flex: 1;
        }

        .item-basic h2 {
            margin: 0 0 10px 0;
            color: #06b6d4;
        }

        .item-basic p {
            margin: 5px 0;
            color: #94a3b8;
        }

        .details-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .detail-item {
            background: rgba(255,255,255,0.02);
            padding: 15px;
            border-radius: 8px;
            border-right: 3px solid #7c3aed;
        }

        .detail-label {
            font-weight: bold;
            color: #06b6d4;
            margin-bottom: 5px;
        }

        .detail-value {
            color: #e6eef6;
        }

        .codes-section {
            margin-bottom: 30px;
        }

        .codes-section h3 {
            color: #06b6d4;
            margin-bottom: 15px;
        }

        .code-item {
            background: rgba(255,255,255,0.02);
            padding: 10px 15px;
            border-radius: 6px;
            margin-bottom: 8px;
            display: inline-block;
            margin-left: 10px;
        }

        .actions {
            display: flex;
            gap: 15px;
            justify-content: center;
            margin-top: 30px;
        }

        .btn {
            display: inline-block;
            padding: 12px 24px;
            border-radius: 8px;
            text-decoration: none;
            font-weight: bold;
            cursor: pointer;
            border: none;
            transition: all 0.3s;
        }

        .btn-primary {
            background: linear-gradient(90deg, #06b6d4, #7c3aed);
            color: #021024;
        }

        .btn-secondary {
            background: rgba(255,255,255,0.1);
            color: #e6eef6;
            border: 1px solid rgba(255,255,255,0.2);
        }

        .btn:hover {
            opacity: 0.9;
            transform: translateY(-2px);
        }

        .badge {
            display: inline-block;
            padding: 6px 12px;
            border-radius: 6px;
            font-size: 14px;
            font-weight: bold;
        }

        .badge-success {
            background: #10b981;
            color: #fff;
        }

        .badge-danger {
            background: #ef4444;
            color: #fff;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>تفاصيل المنتج</h1>

        <div class="item-header">
            @if (Model.Images != null && Model.Images.Any())
            {
                var firstImage = Model.Images.First();
                <img src="data:image/jpeg;base64,@Convert.ToBase64String(firstImage.ItemImageData)" 
                     alt="@Model.Name" class="item-image">
            }
            else
            {
                <div class="item-image" style="background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%); display: flex; align-items: center; justify-content: center; font-size: 48px; color: white;">
                    📦
                </div>
            }
            <div class="item-basic">
                <h2>@Model.Name</h2>
                <p><strong>رقم المنتج:</strong> #@Model.Id</p>
            </div>
        </div>

        <div class="details-grid">
            <div class="detail-item">
                <div class="detail-label">الشركة المصنعة</div>
                <div class="detail-value">@(Model.Company?.Name ?? "غير محدد")</div>
            </div>

            <div class="detail-item">
                <div class="detail-label">المخزن الافتراضي</div>
                <div class="detail-value">@(Model.Store?.Name ?? "غير محدد")</div>
            </div>

            <div class="detail-item">
                <div class="detail-label">الوحدة</div>
                <div class="detail-value">@(Model.Unit?.Name ?? "غير محدد")</div>
            </div>

            <div class="detail-item">
                <div class="detail-label">سعر الشراء</div>
                <div class="detail-value">@(Model.BuyPrice?.ToString("N2") ?? "-") ريال</div>
            </div>

            <div class="detail-item">
                <div class="detail-label">سعر البيع</div>
                <div class="detail-value">@(Model.SalePrice?.ToString("N2") ?? "-") ريال</div>
            </div>

            <div class="detail-item">
                <div class="detail-label">الحد الأدنى للكمية</div>
                <div class="detail-value">@(Model.MinimumQuantity?.ToString() ?? "غير محدد")</div>
            </div>

            <div class="detail-item">
                <div class="detail-label">الحد الأدنى للبيع</div>
                <div class="detail-value">@(Model.MinQuantitySale?.ToString() ?? "غير محدد")</div>
            </div>

            <div class="detail-item">
                <div class="detail-label">حالة الشراء</div>
                <div class="detail-value">
                    @if (Model.IsActiveBuy == true)
                    {
                        <span class="badge badge-success">مفعل</span>
                    }
                    else
                    {
                        <span class="badge badge-danger">غير مفعل</span>
                    }
                </div>
            </div>

            <div class="detail-item">
                <div class="detail-label">حالة البيع</div>
                <div class="detail-value">
                    @if (Model.IsActiveSale == true)
                    {
                        <span class="badge badge-success">مفعل</span>
                    }
                    else
                    {
                        <span class="badge badge-danger">غير مفعل</span>
                    }
                </div>
            </div>

            <div class="detail-item">
                <div class="detail-label">منع الكسور</div>
                <div class="detail-value">
                    @if (Model.PreventFraction)
                    {
                        <span class="badge badge-success">نعم</span>
                    }
                    else
                    {
                        <span class="badge badge-danger">لا</span>
                    }
                </div>
            </div>

            <div class="detail-item">
                <div class="detail-label">منع الخصم</div>
                <div class="detail-value">
                    @if (Model.PreventDiscount)
                    {
                        <span class="badge badge-success">نعم</span>
                    }
                    else
                    {
                        <span class="badge badge-danger">لا</span>
                    }
                </div>
            </div>
        </div>

        @if (Model.Codes != null && Model.Codes.Any())
        {
            <div class="codes-section">
                <h3>أكواد المنتج</h3>
                @foreach (var code in Model.Codes)
                {
                    <div class="code-item">
                        🏷️ @code.ItemCodeValue
                    </div>
                }
            </div>
        }

        <div class="actions">
            <a href="@Url.Action("Edit", new { id = Model.Id })" class="btn btn-primary">تعديل البيانات</a>
            <a href="@Url.Action("List")" class="btn btn-secondary">العودة للقائمة</a>
        </div>
    </div>
</body>
</html>
